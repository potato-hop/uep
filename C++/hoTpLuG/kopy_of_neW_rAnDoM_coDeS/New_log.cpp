#include <iostream>
using namespace std;
int main()
{
	int a, b, x, z = 3, m = 0;
	const p = 3;
	cin >> a >> b;
	x = a;
	while(b > x)
	{
		while(b >= z)
		{
			if(z <= b && z >= a)
			{
				cout << z << endl;
				m++;
				break;
			}
			z = z * p;
		}
		if(m > 0)
			break;
		while()
		{
			if( % 3 == 0)
				break;
		}
	}
}
